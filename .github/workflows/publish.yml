name: Publish to npm

on:
  push:
    tags:
      - 'v*'

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: write # Required for creating releases
      id-token: write # Required for OIDC and provenance
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '24'
          registry-url: 'https://registry.npmjs.org'
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build
        run: pnpm run build

      - name: Publish
        run: npm publish --provenance --access public --registry=https://registry.npmjs.org/
        env:
          NPM_CONFIG_PROVENANCE: true

      - name: Extract Release Notes
        id: extract_release_notes
        run: |
          # Extract the content of the first section (most recent version) from CHANGELOG.md
          # This skips the first header and stops before the second header
          awk '/^## \[/{count++; if (count == 2) exit; next} count == 1' CHANGELOG.md > release_notes.md
          echo "NOTES_FILE=release_notes.md" >> $GITHUB_ENV

      - name: Create Release
        uses: softprops/action-gh-release@v2
        if: github.ref_type == 'tag'
        with:
          body_path: release_notes.md
